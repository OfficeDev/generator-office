steps:
  - task: CmdLine@2
    inputs:
      script: |
        echo Running Tests
        
        npm install office-addin-manifest -g
        npm run test
            
        echo Done running tests
    displayName: 'Run Tests'
