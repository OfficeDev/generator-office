# Yo Office 1.0

```
    # to install typings use:
    typings install dt~<lib name> --global --save

    # to reinstall just run
    typings install

    # to install deps
    npm install
```